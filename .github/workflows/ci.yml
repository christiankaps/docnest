name: CI

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Print Xcode version
        run: xcodebuild -version

      - name: Build
        run: |
          xcodebuild \
            -project DocNest.xcodeproj \
            -scheme DocNest \
            -configuration Debug \
            -destination 'platform=macOS' \
            build

      - name: Run Unit Tests
        run: |
          xcodebuild \
            -project DocNest.xcodeproj \
            -scheme DocNest \
            -configuration Debug \
            -destination 'platform=macOS' \
            -only-testing:DocNestTests \
            -resultBundlePath "$RUNNER_TEMP/UnitTestResults.xcresult" \
            test

      - name: Run UI Tests
        run: |
          xcodebuild \
            -project DocNest.xcodeproj \
            -scheme DocNest \
            -configuration Debug \
            -destination 'platform=macOS' \
            -only-testing:DocNestUITests \
            -resultBundlePath "$RUNNER_TEMP/UITestResults.xcresult" \
            test

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: TestResults
          path: ${{ runner.temp }}/*.xcresult
          retention-days: 7
